version: '2'
services:
  findep-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Findep/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Findep
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
